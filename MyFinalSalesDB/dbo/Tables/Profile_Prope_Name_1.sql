CREATE TABLE [dbo].[Profile_Prope_Name] (
    [ID]   INT           IDENTITY (1, 1) NOT NULL,
    [Name] NVARCHAR (70) NOT NULL,
    CONSTRAINT [PK_Profile_Prope_Name] PRIMARY KEY CLUSTERED ([ID] ASC)
);

